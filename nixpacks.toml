providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install", "cd server && npm install && cd .."]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node server/index.js"
